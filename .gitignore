# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/

# Environment / secrets (examples only are committed)
.env
.env.*
!backend/.env.example
!frontend/.env.example

# Node / frontend
node_modules/
frontend/dist/
*.local

# Local private file stores (COR files must never be committed)
backend/var/
graphify-out/

# Local AI-tool state (project config lives in .kilo/ and AGENTS.md only)
.opencode/
.kilocode/

# OneDrive / OS noise
.DS_Store
Thumbs.db
desktop.ini
