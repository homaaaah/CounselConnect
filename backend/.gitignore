# CounselConnect backend ignores
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/

# Real environment files (only .env.example is committed)
.env
.env.*
!.env.example

# Local private file stores (COR files must never be committed)
var/
*.sqlite3

# Stray npm artifacts (Python backend)
package.json
package-lock.json
node_modules/

# OS noise
.DS_Store
Thumbs.db
