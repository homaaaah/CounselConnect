"""Backend test package.

Layout per project instructions: unit/ (services, domain rules),
integration/ (module + DB interactions), api/ (HTTP contract tests).
File naming mirrors targets per NAMING_CONVENTIONS.md (test_<module>.py).
"""
