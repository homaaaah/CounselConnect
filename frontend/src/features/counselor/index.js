/**
 * Feature module: counselor (dashboard, verification queue, operations).
 * Guidance Staff sees ONLY assigned COR verification
 * (docs/USER_ROLES.md — enforced authoritatively in the backend).
 */
export {};
