/**
 * Feature module: sos (five-question triage flow, counselor case handling).
 * The optional Observed Expression Cue is session-only, device-local, and
 * never affects SOS scoring (docs/AI_EMOTIONAL_BASELINE.md).
 */
export {};
