/**
 * Feature module: content (landing/home content display).
 * Reads PUBLISHED CMS blocks, FAQs, announcements, and active emergency
 * contacts from the content module (ERD: content_items, emergency_contacts).
 */
export { usePublicContent } from "./usePublicContent";
