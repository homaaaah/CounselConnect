# CounselConnect frontend environment template.
# Copy to `.env` locally; never commit real values.

# Backend API base (FastAPI serves /api/v1).
VITE_API_BASE_URL=http://localhost:8000/api/v1

# NOTE: No auth-related variables until ADR-P01 approves the mechanism.
